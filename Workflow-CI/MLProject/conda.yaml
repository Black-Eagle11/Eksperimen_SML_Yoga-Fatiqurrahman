name: heart-disease-ml-ci
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.12
  - pip
  - pip:
      - numpy
      - pandas
      - scikit-learn
      - mlflow
      - joblib
      - pyyaml